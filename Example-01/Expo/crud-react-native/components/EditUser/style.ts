import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    container: {
        flex: 1,
        borderColor: '#000',
        borderWidth: 2,
        borderRadius: 10,
        padding: 15,
    },
    editArea: {
        marginBottom: 15
    },
    input: {
        borderColor: '#000',
        borderWidth: 1,
        paddingLeft: 4
    }
});

export default styles;